// Write your code here

import MovieItem from '../MovieItem'

import './index.css'

const MovieSlider = props => (
  <div>
    <MovieItem itemDetail={props} />
  </div>
)

export default MovieSlider
